# 单调栈问题

寻找左边/右边第一个最大/小问题

| 要寻找的目标             | 遍历方向     | 应维护的栈单调性 (栈底 -> 栈顶) | 核心逻辑与类比                                               |
| :----------------------- | :----------- | :------------------------------ | :----------------------------------------------------------- |
| **右边第一个更大的元素** | **从左向右** | **单调递减**                    | **后来者清算**：新元素（右）是“法官”，清算（弹出）栈中所有比它小且在它左边的“等待者”，并为它们提供答案。 |
|                          | **从右向左** | **单调递减**                    | **维护候选名单**：栈内保存右侧已遍历的、递增的“更大值”候选。当前元素向左看，名单里第一个（栈顶）就是答案。 |
| **右边第一个更小的元素** | **从左向右** | **单调递增**                    | **后来者清算**：新元素（右）清算栈中所有比它大且在它左边的“等待者”。 |
|                          | **从右向左** | **单调递增**                    | **维护候选名单**：栈内保存右侧已遍历的、递减的“更小值”候选。 |
| **左边第一个更大的元素** | **从左向右** | **单调递减**                    | **维护候选名单**：栈内保存左侧已遍历的、递减的“更大值”候选。当前元素向右看，名单里第一个就是答案。 |
|                          | **从右向左** | **单调递减**                    | **后来者清算**：新元素（左）清算栈中所有比它小且在它右边的“等待者”。(不常用) |
| **左边第一个更小的元素** | **从左向右** | **单调递增**                    | **维护候选名单**：栈内保存左侧已遍历的、递增的“更小值”候选。 |
|                          | **从右向左** | **单调递增**                    | **后来者清算**：新元素（左）清算栈中所有比它大且在它右边的“等待者”。(不常用) |

**单调栈单调性与最大最小相反**

右边第一个最大的值

从左往右遍历，维护递减栈。当遇到违反了递减律的，即遇到比栈顶元素大的，则说明栈顶对应的位置第一个最大值是当前元素。**栈里面存放的是没有确定的**

从右往左遍历，维护递减栈。当遇到违反了递减律的，一直弹出直到栈顶大于当前元素。当前元素位置的第一个大值对应着栈顶元素。**栈里面存放的是候选**



左边第一个最大的值

从左往右遍历，维护递减栈。当遇到违反递减律的，一直弹出直到栈顶大于当前元素。当前元素位置的第一个大值对应着栈顶元素。

从右往左遍历，维护递减栈。当遇到违反了递减律的，即遇到比栈顶元素大的，则说明栈顶对应的位置第一个最大值是当前元素。



还有一类是：先增后减和先减后增，找这个区间

先减后增，找两端的第一个大值。设立递减栈，从左往右，遇到了违反递减律的，栈顶元素的右边界是当前元素，左边界是栈中下一个元素。

```c++
class MonotonicStackTemplate {
public:
    int solve(vector<int>& nums) {
        // 1. 定义栈（存下标！）
        stack<int> st;
        
        // 2. 左哨兵：避免反复判断 !st.empty()
        // -1 代表“左边界之外的虚拟位置”，计算宽度时 width = i - (-1) - 1 = i
        st.push(-1);

        // 3. 右哨兵 (视题目逻辑而定！)
        // 仅当“栈内剩余元素需要强制结算”时才加
        if (is_rectangle_problem) { 
            nums.push_back(0); 
        }
        // 接雨水问题千万别加！因为剩下的就是右边没有比它更高的边界，都会流走，存不了水

        int res = 0; // 结果变量

        // 4. 遍历元素 (包含右哨兵)
        for (int i = 0; i < nums.size(); i++) {
            
            // Case 1: 破坏单调性 -> 触发计算
            // 【核心】：根据题目选择 > 还是 <
            // 找两边第一个比我小的 -> 用递增栈 -> while (nums[i] < nums[st.top()])
            // 找两边第一个比我大的 -> 用递减栈 -> while (nums[i] > nums[st.top()])
            while (st.top() != -1 && nums[i] < nums[st.top()]) {
                // A. 锁定嫌疑人 (当前要计算的主角)
                int mid = st.top();
                st.pop();
                
                // B. 锁定左边界 (嫌疑人左边那个)
                int left = st.top();
                
                // C. 锁定右边界 (当前的刽子手)
                int right = i;

                // D. 业务逻辑计算 (填空题)
                // 例如最大矩形: h = nums[mid], w = right - left - 1
                // 例如接雨水: h = min(nums[left], nums[right]) - nums[mid], w = ...
                // res = max(res, ...);
            }

            // Case 2: 【平底优化】遇到相等高度
            // 只保留最右边的一个，因为它的延展性最强/能避免无效计算
            if (st.top() != -1 && nums[i] == nums[st.top()]) {
                st.pop();
                // 此时不要 continue，要让新下标 i 入栈
            }

            // Case 3: 满足单调性 -> 入栈
            st.push(i);
        }

        return res;
    }
};
```

