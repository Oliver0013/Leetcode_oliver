>这份笔记非常**硬核（Hardcore）\**且\**实战导向**。
>
>你现在的笔记风格属于**“算法解题手册”**（Cheat Sheet），而不是教科书。这种风格对于研究生阶段准备算法面试（LeetCode/机试）非常高效，因为它直接提炼了**模板、状态定义、初始化陷阱**。
>
>针对你的问题：“**需要合并一些信息吗？**” 以及上一条对话中提到的 **“轮廓线DP是什么”**，我有以下几个具体的优化和合并建议。
>
>------
>
>### 1. 结构优化：逻辑流的梳理
>
>你目前的笔记顺序是 `背包 -> 回溯 -> DP序列`。我建议重新调整逻辑流，使其符合人类认知的**“从暴力到优化”**的过程，同时把**“轮廓线DP”**作为一个进阶模块插进去。
>
>**建议的新目录结构：**
>
>1. **回溯 (Backtracking)**
>   - 核心心法（你笔记里的三种模式：侠客、经理、探路）
>   - *进阶：状态压缩回溯（为后面的DP做铺垫）*
>2. **从回溯到 DP 的桥梁**
>   - 你笔记里的“机械翻译法”（这是非常棒的过渡章节）
>3. **经典 DP 模型**
>   - **背包模型**（01、完全、恰好装满）
>   - **序列/字符串模型**（LIS, LCS, 编辑距离）
>4. **进阶 DP (高维/网格)**
>   - 状态压缩 DP (基于回溯中的掩码思想)
>   - **轮廓线 DP (你缺的这块拼图)**
>
>------
>
>### 2. 内容除虫：修复笔记中的重复
>
>在你的**“4. 求最小代价”**章节中，有一段文字完全重复了。
>
>> **待修复：** 在 `dp[j] = min(...)` 代码块的下方，有一段以 `**适用场景**：问“最少需要多少个物品...` 开头的文字，和代码块上方的文字完全一样。建议删除代码块下方的重复段落，保持整洁。
>
>------
>
>### 3. 新增模块：什么是轮廓线 DP？（Plug-in for your notes）
>
>既然你问到了**轮廓线 DP (Broken Profile DP)**，这是解决**网格（Grid）覆盖/填充类问题**的终极武器。它和你在回溯里写的“状态压缩”是近亲，但更精细。
>
>你可以把下面这段**直接复制**补充到你的笔记中（建议放在“状态压缩”或“进阶技巧”部分）：
>
>#### 🧩 补充笔记：轮廓线 DP (Broken Profile DP)
>
>核心痛点：
>
>在 $N \times M$ 的网格中进行状态压缩 DP，如果直接压缩整行（状态 $2^M$），当 $M$ 较大但 $N$ 很大时，转移需要枚举上一行的所有状态，复杂度较高。
>
>核心思想：
>
>不按“整行”转移，而是逐格（Cell by Cell）转移。
>
>我们在填第 $(i, j)$ 格时，不需要知道整行的情况，只需要知道**“轮廓线”**上的状态。
>
>什么是轮廓线？
>
>当我们处理到第 $(i, j)$ 格时，轮廓线是指：
>
>- 当前格 $(i, j)$ 左边的部分（已处理的新一行）
>- 当前格 $(i, j)$ 上方及右边的部分（未处理的旧一行）
>- **状态定义**：`dp[cur][mask]`，其中 `mask` 记录的是轮廓线上 $M$ 个位置的插头/覆盖状态。
>
>**对比**：
>
>- **普通状压 DP**：一次跳一行。复杂度 $O(N \cdot 2^{2M})$ （也就是 $4^M$）。
>- **轮廓线 DP**：一次跳一格。我们只决策当前格子放不放、怎么放，只影响 mask 中的一两个 bit。复杂度降为 $O(N \cdot M \cdot 2^M)$。
>
>**经典例题**：
>
>- **骨牌铺方格**（用 $1 \times 2$ 的骨牌铺满 $N \times M$）：POJ 2411 / 蒙德里安的梦想。
>- **LCP 4. 覆盖**。
>
>------
>
>### 4. 深度融合：把“回溯”和“DP”打通
>
>你笔记里有一个非常好的点是 “1411. 给N x 3网格图涂色的方案数”。
>
>你目前把它归类在回溯里。实际上，这道题是连接回溯和状态压缩 DP的完美案例。
>
>建议合并操作：
>
>在你的“回溯 -> 经典案例”表格下，或者“状态压缩”章节下，加一句批注：
>
>> 💡 思维升维：
>>
>> 对于 1411 这种题，如果 $N$ 很大（比如 $N=5000$），回溯会超时（即使加了记忆化，栈深度也太深）。
>>
>> 这时必须使用状态压缩 DP。
>>
>> - 回溯写法：`dfs(row, current_mask)`
>> - DP 写法：`dp[row][current_mask]`
>> - **本质一样**，只是将递归栈换成了循环迭代，避免 Stack Overflow。
>
>------
>
>### 5. 总结你的笔记（Assessment）
>
>你的笔记现在**非常好**，主要体现在：
>
>1. **“恰好装满”的初始化总结**：这是大多数人容易搞混的，你总结的 `0`, `-INF`, `+INF` 非常精准。
>2. **回溯的三种模式**：侠客/经理/探路，形象且易记，有助于在考场上快速选择模板。
>3. **机械翻译法**：这是工程化思维的体现，能帮你快速把写出来的 DFS 改成 DP。
>
>下一步行动 (Action Item)：
>
>建议把上面关于 “轮廓线 DP” 的解释加入笔记，并删掉**“求最小代价”部分的重复段落。如果你准备面试，重点复习背包的初始化和序列DP的三种状态定义**，这两块是出现率最高的。
>
>



# 01背包问题

有个背包，最多能装4，三个物品的信息如下：

| 物品 | 重量 | 价值 |
| ---- | ---- | ---- |
| 0    | 1    | 15   |
| 1    | 3    | 20   |
| 2    | 4    | 30   |

怎么装价值最多？最多价值是多少？

## 二维dp方法

| 物品\背包数 | 0    | 1    | 2    | 3    |
| ----------- | ---- | ---- | ---- | ---- |
| 0           | 0    | 15   | 15   | 15   |
| 1           | 0    |      |      |      |
| 2           | 0    |      |      |      |

dp[i] [j]数组含义为：[0, i]的物品任取，放在容量为j的背包里的最大价值；

初始化：第一列全为0，第一行：重量<=1?15; 否则为0

递推公式：dp[i] [j] = max(dp[i-1] [j], dp[i-1] [j - weight[i]] + Value[i])

含义：在i-1的基础上增加第i个物品，那么下一个格子可以选择加进去，或者不加进去。

在表格里则是涉及到正上方的值和左上方的值进行递推。

```c++
//遍历物品，即遍历行
for (int i = 0; i < item_num; i++){
    //遍历背包
    for (int j=0; j < bag_load; j++){
    	dp[i][j] = max(dp[i-1][j],dp[i-1][j-weight[i]]+Value[i]);    
    }    
}
```



## 一维dp方法

实际上就是滚动数组，本质还是两层循环，将上一层的行到下一行重新开始遍历，

然后原本利用正上方和左上方的数值进行递推，变成了左方和当前的数值进行迭代（注意反向遍历，避免重复）

---

dp[j] 数组含义为：容量为j的背包装的最大价值是？

初始化：全部初始化为0

dp[j] = max(dp[j], dp [j - weight[i]] + Value[i])

```c++
//遍历物品，即遍历行
for (int i = 0; i < item_num; i++){
    //遍历背包，反向遍历，避免物品重复
    for (int j=bag_load; j >= weight[i]; j--){
    	dp[j] = max(dp[j], dp[j - weight[i]] + Value[i]);    
    }    
}
```



# 完全背包问题

01背包基础上，可以重复装物品，所以只要改成正向遍历即可，注意第二个遍历的起点：

```c++
//遍历物品，即遍历行
for (int i = 0; i < item_num; i++){
    //遍历背包，反向遍历，避免物品重复
    for (int j=weight[i]; j <= bag_load; j++){
    	dp[j] = max(dp[j], dp[j - weight[i]] + Value[i]);    
    }    
}
```



# 恰好装满的背包问题

这类问题要求背包**必须被装满**，如果有剩余空间则认为无效。

### 1. 求可行

适用场景：只问“能不能”装满，且物品的重量和价值可以等价（如分割等和子集）。

核心逻辑：让 Value[i] = Weight[i]。

这样问题就变成了：在容量限制下，求最大重量。如果最大重量等于背包容量，说明恰好装满了。

- **初始化**：`dp` 数组全部初始化为 0。
- **递推公式**：`dp[j] = max(dp[j], dp[j - weight[i]] + weight[i])`
- **判断结果**：`return dp[target] == target`

### 2. 求最大价值

适用场景：物品有独立的价值，题目求“在恰好装满的前提下，最大价值是多少？”（如果装不满返回 -1 或不可达）。

核心逻辑：通过非法状态的初始化来区分“装满了”和“没装满”。

- **初始化**：
  - `dp[0] = 0` （容量为0，价值为0，这是唯一的合法起始状态）。
  - `dp[1...target]` = **负无穷** (例如 `INT_MIN` 或 `-0x3f3f3f3f`)。
  - *解释*：如果一个状态是由负无穷推导过来的，说明它之前没有被恰好装满过，所以它本身也是非法的。
- **递推公式**：
  - `dp[j] = max(dp[j], dp[j - weight[i]] + Value[i])`
- **判断结果**：
  - 如果 `dp[target] < 0` (或者仍为负无穷)，说明无法恰好装满。
  - 否则 `dp[target]` 即为恰好装满时的最大价值。

C++

```
// 初始化：除了 dp[0] 其他都是负无穷
vector<int> dp(bag_load + 1, INT_MIN);
dp[0] = 0;

for (int i = 0; i < item_num; i++) {
    for (int j = bag_load; j >= weight[i]; j--) {
        // 只有当前面是合法状态时，才进行累加
        if (dp[j - weight[i]] != INT_MIN) {
             dp[j] = max(dp[j], dp[j - weight[i]] + Value[i]);
        }
    }
}
```

### 3. 求方案数 (组合/排列)

适用场景：问“有多少种方法可以恰好装满背包？”（例如 LeetCode 494. 目标和, 518. 零钱兑换 II）。

核心逻辑：不再求 max，而是求 sum。

- **初始化**：`dp[0] = 1` （装满容量为0的背包，只有一种方法：什么都不装）。
- **递推公式**：
  - `dp[j] += dp[j - weight[i]]`
  - *含义*：凑成容量 `j` 的方案数 = 不选当前物品的方案数 + 选当前物品(由 `j-weight` 推过来)的方案数。

**注意遍历顺序（决定是组合还是排列）：**

- **组合数 (不考虑顺序，如硬币组合)**：

  - 先遍历物品，再遍历背包。
  - 例如：`{1, 5}` 和 `{5, 1}` 视为一种。

  ```C++
  // 初始化：除了 dp[1] 其他都是0
  vector<int> dp(bag_load + 1, INT_MIN);
  dp[0] = 1;
  
  for (int i = 0; i < item_num; i++) { // 物品在外
      for (int j = weight[i]; j <= bag_load; j++) {
          dp[j] += dp[j - weight[i]];
      }
  }
  ```

- **排列数 (考虑顺序，如爬楼梯)**：

  - 先遍历背包，再遍历物品。
  - 例如：先走1步再走2步，和先走2步再走1步，是不同的方案。

  ```C++
  for (int j = 0; j <= bag_load; j++) { // 背包在外
      for (int i = 0; i < item_num; i++) {
          if (j >= weight[i]) dp[j] += dp[j - weight[i]];
      }
  }
  ```

### 4. 求最小代价

适用场景：问“最少需要多少个物品/多少成本才能恰好装满？”（例如 LeetCode 322. 零钱兑换, 279. 完全平方数）。

核心逻辑：求 min，初始化的“屏障”必须是正无穷。

初始化：

- `dp[0] = 0`（装满0容量，代价为0，这是合法的）。

- `dp[1...target]` = **正无穷** (例如 `INT_MAX` 或 `amount + 1`)。

  - 解释：min 操作会取更小的值，如果初始化为 0，所有结果都会被覆盖成 0。必须设为最大值，才能在比较中被有效的较小值替换。

    递推公式：

    dp[j] = min(dp[j], dp[j - weight[i]] + Value[i])

- 注意：如果是求硬币数量，这里的 Value[i] 通常是 1。

  判断结果：

- 如果 `dp[target]` 仍然是正无穷（或大于 amount），说明无法恰好装满，返回 -1。

- 否则返回 `dp[target]`。

```C++
// 初始化：除了 dp[0] 其他都是正无穷 (或者 amount + 1)
vector<int> dp(bag_load + 1, INT_MAX);
dp[0] = 0;

for (int i = 0; i < item_num; i++) {
    // 如果是完全背包，这里 j 正向遍历；如果是 0/1，则反向
    for (int j = weight[i]; j <= bag_load; j++) {
        // 只有当前面是合法状态时 (不是正无穷)，才进行推导
        if (dp[j - weight[i]] != INT_MAX) {
             dp[j] = min(dp[j], dp[j - weight[i]] + Value[i]);
        }
    }
}
```

------



**适用场景**：问“最少需要多少个物品/多少成本才能恰好装满？”（例如 LeetCode 322. 零钱兑换, 279. 完全平方数）。 **核心逻辑**：求 `min`，初始化的“屏障”必须是**正无穷**。 **初始化**：

- `dp[0] = 0`（装满0容量，代价为0，这是合法的）。
- `dp[1...target]` = **正无穷** (例如 `INT_MAX` 或 `amount + 1`)。
  - *解释*：`min` 操作会取更小的值，如果初始化为 0，所有结果都会被覆盖成 0。必须设为最大值，才能在比较中被有效的较小值替换。 **递推公式**： `dp[j] = min(dp[j], dp[j - weight[i]] + Value[i])`
- *注意*：如果是求硬币数量，这里的 `Value[i]` 通常是 1。 **判断结果**：
- 如果 `dp[target]` 仍然是正无穷（或大于 amount），说明无法恰好装满，返回 -1。
- 否则返回 `dp[target]`。

---

### 总结对比

| **问题类型**           | **核心关注点**          | **初始化**                | **递推公式**                    |
| ---------------------- | ----------------------- | ------------------------- | ------------------------------- |
| **0/1 背包 (求最值)**  | 尽量装，不用满          | 全 0                      | `max(dp[j], dp[j-w] + v)`       |
| **恰好装满 (求最大V)** | 必须满，求**最大**V     | `dp[0]=0`, 其它**负无穷** | `max(dp[j], dp[j-w] + v)`       |
| **恰好装满 (求最小V)** | 必须满，求**最小**cost  | `dp[0]=0`, 其它**正无穷** | `min(dp[j], dp[j-w] + v)`       |
| **恰好装满 (求可行)**  | 能不能满 (Value=Weight) | 全 0                      | `max` 后看 `dp[target]==target` |
| **恰好装满 (求方案)**  | 有几种装法              | `dp[0]=1`, 其它 0         | `dp[j] += dp[j-w]`              |



这总结非常必要！把这两个模板刻在脑子里，你可以秒杀 90% 的 DFS/回溯/搜索类题目。

我们要区分的是：**“我要具体的路径（方案）”** 还是 **“我要一个数字（最优解/总数）”**。

------

# 回溯

### 核心心法

**“画出状态树，搞清三件事”**

1. **节点状态**：这一刻，我需要哪几个变量来“定格”世界？(State Definition)，实际上就是 DFS 函数中那些【会随着递归不断改变的参数】。

> **坐标的显隐性**
>
> 1. **容器型状态 (`string`, `vector`)**：自带计数器 (`.size()`)。
>    - $\rightarrow$ `j` (列/步数) 通常是**隐式**的，不需要作为参数传递。
> 2. **数值型状态 (`int` 掩码)**：丢失计数信息。
>    - $\rightarrow$ `j` (列/步数) 必须是**显式**的，必须作为参数传递。
> 3. **几何收敛性**：
>    - 若问题规模随递归自然缩小（如金字塔、归并排序），`i` (层级) 可隐式。
>    - 若问题规模固定（如 $N \times M$ 网格），`i` 必须显式传递。

1. **分支逻辑**：横向怎么遍历？(For Loop)
2. **结果导向**（信号-边/箭头）：是要找一条路(Bool)、所有路(Void)还是最优路(Int)？

### 经典案例

关键是要得到逻辑树模型，理清每个节点的状态和返回值

| 题号 | 题名                      | 类型                      | 节点状态                                                     | 数据结构                               |
| ---- | ------------------------- | ------------------------- | ------------------------------------------------------------ | -------------------------------------- |
| 756  | 金字塔转换矩阵            | bool回溯+状态跃迁         | （"XYZ", "AB"）金字塔当前层和下一层<br />约束是当前层，构建是下一层 | string now, string next                |
| 1411 | 给N x 3网格图涂色的方案数 | int回溯+状态跃迁+状态压缩 | 约束是pre_row颜色状态<br />构建是cur_row颜色状态<br />进度：i，j | int i, int j, int pre_row, int cur_row |



## 1. Void 回溯 (侠客模式)

- **目标**：寻找**所有**可能的解 (全排列、组合、所有路径)。
- **特征**：无返回值，依靠全局 `res` 收集结果。
- **回溯方式**：**显式回溯** (手动 `push` / 手动 `pop`)。

```c++
vector<vector<int>> res;
vector<int> path;

void backtrack(参数) {
    // 1. Base Case (收获结果)
    if (满足结束条件) {
        res.push_back(path);
        return;
    }

    // 2. 横向遍历
    for (选择 : 本层候选集) {
        if (剪枝条件) continue;

        path.push_back(选择);  // 做选择
        backtrack(参数变化);   // 进入下一层
        path.pop_back();       // 撤销选择 (对称！)
    }
}
```

------

## 2. Int 回溯 (经理模式)

- **目标**：寻找**最值** (最长、最短) 或 **方案总数**。
- **特征**：有返回值 (`int`)，必须配合 **Memoization** (记忆化)。
- **回溯方式**：**隐式回溯** (状态变化直接写在递归参数里，如 `index + 1`)。
- **Memo 策略**：**全记录** (成功和失败的结果都要记，因为要比大小)。

```C++
int memo[N][...]; // 初始化为 -1

int dfs(参数) {
    // 1. Base Case
    if (满足结束条件) return 0; // 或 1

    // 2. 查表
    if (memo[参数] != -1) return memo[参数];

    int res = 0; // 初始化 (找最大置MIN，找最小置MAX)

    // 3. 横向遍历
    for (选择 : 本层候选集) {
        // 汇总结果 (累加 或 打擂台)
        res = max(res, 贡献值 + dfs(参数变化));
    }

    // 4. 记账并返回
    return memo[参数] = res;
}
```

------

## 3. Bool 回溯 (探路模式)

- **目标**：寻找**可行性** (是否存在一条路、能不能拼成)。
- **特征**：有返回值 (`bool`)，找到一个 `true` 立刻连锁返回。
- **Memo 策略**：**只记失败 (Fail-only)**。成功直接返回，只有死路才需要记录防止重蹈覆辙。

```C++
unordered_set<string> deadEnds; // 只记录“死胡同”

bool dfs(参数) {
    // 1. Base Case (通关)
    if (满足结束条件) return true;

    // 2. 查表 (只查是不是死路)
    if (deadEnds.count(参数)) return false;

    // 3. 横向遍历
    for (选择 : 本层候选集) {
        // --- 核心区别：一旦子节点成功，立刻上报 ---
        if (dfs(参数变化)) return true; 
    }

    // 4. 记死路并返回
    deadEnds.insert(参数); 
    return false;
}
```

------

## 🚀 进阶技巧1：状态跃迁 (State Leap)

**适用场景**：多维结构、阶段性任务。

- **金字塔/倒三角**：填满当前层 $\to$ 跃迁到下一层。
- **二维网格/棋盘**：填满当前行 $\to$ 跃迁到下一行 (即 `(r, c)` 到 `(r, c+1)`，当 `c` 越界时触发大跃迁)。

**核心逻辑**：

1. **量变 (横向遍历)**：在当前维度 (Layer/Row) 进行常规的递归或循环。
2. **质变 (跃迁点)**：当检测到当前维度的任务完成时，**递归参数发生突变**，控制权移交到下一个维度，同时**重置**局部参数。

C++

```C++
bool dfs(string now, string next) {
    // Base Case
    if (now.size() == 1) return true;

    // --- 状态跃迁 (关键点) ---
    // 当前层(next)已填满，跃迁到下一层，now 变为 next，next 清空
    if (next.size() == now.size() - 1) {
        return dfs(next, ""); // ⚠️ 必须 return！移交控制权
    }

    // --- 常规逻辑 ---
    // 继续在当前层(next)横向填充...
}
```

------

## 🚀 进阶技巧 2：状态压缩 (State Compression)

**核心心法**

**“降维打击：将复杂的对象 (Vector/String) 拍扁成一个整数 (Int)。”**

- **痛点**：`vector<int>` 或 `string` 作为状态时，拷贝慢、比较慢、无法直接作为数组索引（只能用慢速的 `map`）。
- **原理**：利用 **$k$ 进制编码** 或 **位运算**，将多个小状态“拼”进一个整型变量中。

### 1. 什么时候用？

当你的**“节点状态”**中包含一个**短小且定长**的序列时：

- **网格图的一行**：如 1411 (3列，每列3种颜色)。
- **集合的子集**：如 状态压缩 DP (N < 20，每个元素选或不选)。
- **棋盘轮廓**：如 蒙德里安的梦想。

### 2. 怎么压？(Packing)

**公式：** `Mask = Σ (Value[i] << (i * 位宽))`

以 1411 题为例（3列，每列 3 色 0,1,2）：

由于 3 种颜色需要 2 个 bit ($2^1 < 3 < 2^2$)，我们把 int 当作一个**“存放 2-bit 数据的数组”**。

```C++
// 假设第0列是红(0)，第1列是黄(1)，第2列是绿(2)
// 二进制视角： ... [Col 2] [Col 1] [Col 0]
//               ...   10      01      00
int mask = 0;
mask |= (0 << 0);       // 第0列放入低2位
mask |= (1 << 2);       // 第1列放入中2位 (左移1*2)
mask |= (2 << 4);       // 第2列放入高2位 (左移2*2)

// 此时 mask = 36 (十进制) = 100100 (二进制)
```

### 3. 怎么解？(Unpacking)

**公式：** `Value[i] = (Mask >> (i * 位宽)) & 掩码`

```C++
// 取出第 j 列的颜色
// 1. 右移：把第 j 列的数据移到最右边
// 2. 与运算：只保留最低的2位 (& 3，即二进制 11)
int color = (mask >> (j * 2)) & 3;
```

### 4. 为什么要这样？(ROI 分析)

| **特性**     | **不压缩 (Vector/String)**     | **压缩 (Int Mask)**           |
| ------------ | ------------------------------ | ----------------------------- |
| **空间开销** | 堆内存分配，大                 | 寄存器/栈内存，极小           |
| **拷贝速度** | $O(K)$                         | $O(1)$                        |
| **记忆化**   | 必须用 `map<vector, int>` (慢) | 可用 `memo[mask]` 数组 (极快) |
| **状态查询** | 循环遍历                       | 位运算 ($O(1)$)               |

### ⚔️ 终极对比清单

| **特性**     | **Void (找所有)** | **Int (找最值)**   | **Bool (找可行)**   |
| ------------ | ----------------- | ------------------ | ------------------- |
| **关注点**   | 路径 (Path)       | 数值 (Val)         | 结局 (True/False)   |
| **回溯写法** | 显式 (`pop_back`) | 隐式 (参数传值)    | 显式/隐式皆可       |
| **返回值**   | `void`            | `int`              | `bool`              |
| **Memo策略** | **不用** (通常)   | **全记录** (必用)  | **只记失败** (优化) |
| **典型题**   | 全排列、N皇后     | 零钱兑换、打家劫舍 | 单词搜索、金字塔    |

这是为你总结的**《从记忆化搜索到 DP：机械翻译手册》**，主打一个“无脑转换”。

------

## 🚀 进阶技巧 3：从记忆化搜索到 DP (机械翻译法)

### 1. 核心心法

**“DP 不是重新设计算法，而是对记忆化搜索的‘逆向重构’。”**

- **记忆化 (Top-Down)**：懒计算，我要什么就算什么 (Pull)。
- **迭代 DP (Bottom-Up)**：地毯式，先把基石填好再往上搭 (Push)。
- **本质**：同一张计算图 (Computation Graph)，只是**遍历方向相反**。

### 2. 六步机械翻译 (Translation Protocol)

对照着写好的 DFS 代码，执行以下查找替换：

| **步骤** | **核心逻辑**            | **记忆化搜索 (DFS + Memo)**                                  | **迭代 DP (Iterative DP)**                                   |
| -------- | ----------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **1**    | **容器**                | `memo` 初始化为 `-1` (懒加载)                                | `dp` 初始化为 `0` 或 `INF` (初始累积值)                      |
| **2**    | **起点**                | 函数开头的 `Base Case` `if (i==0) return 1;`                 | 循环外的 `Initialization` `dp[0] = 1;`                       |
| **3**    | **方向**                | 递归调用 `dfs(i-1)` (自顶向下，从 N 找 0)                    | 主循环 `for (i = 1; i <= n)` (自底向上，从 1 推 N)           |
| **4**    | **重置** *(Reset)*      | **函数参数传空值** `dfs(..., 0)` *(每层递归的局部变量自动是新的)* | **循环内声明新数组** `vector new_dp(Size, 0);` *(手动清空当前层的画布)* |
| **5**    | **累加** *(Transition)* | **返回值累加** `res += dfs(...)`                             | **数组赋值** `new_dp[cur] += dp[pre]`                        |
| **6**    | **跃迁** *(The Leap)*   | **递归参数轮换** `return dfs(i-1, cur_row)` *(把现在的 cur 传给下一层的 pre)* | **滚动数组更新** `dp = new_dp;` *(本行计算结束，强制覆盖上一行数据)* |

### 3. 难点翻译：状态跃迁 (The Leap)

当递归中出现**参数身份互换** (如金字塔下一层变当前层、网格图换行) 时：

- **递归表现**：`return dfs(next_state, ...)` —— **接力棒传递**。
- **DP 表现**：`dp = new_dp;` —— **滚动数组更新**。
  - `new_dp` (下一层) 计算完毕后，赋值给 `dp` (当前层)。
  - 下一轮循环开始前，`new_dp` 清空重置。

### 4. 终极优化：空间压缩 (Space Optimization)

**判定标准**：观察翻译后的方程，`dp[i]` 是否只依赖 `dp[i-1]`？

- **是** $\to$ **降维**。
- **操作**：
  1. 删掉 `dp` 数组的第一维 `[N]`。
  2. 定义两个一维数组：`pre` (旧) 和 `cur` (新)。
  3. 内层循环算 `cur`，外层循环末尾执行 `pre = cur`。

------

### 💡 一句话总结

**把 `dfs` 改成 `dp`，把 `( )` 改成 `[ ]`，把递归改成 `for` 循环，把 `return` 改成 `dp = new_dp`。**



# 搜索问题的选择解法：

#### 第一步：能不能贪心？（最快，O(N log N)）

- **特征**：题目里有没有类似“越大越好”、“越早越好”、“区间不重叠”的暗示？
- **验证**：试举反例。
  - *例子*：现在的选择会影响未来的选择吗？如果我选了当前最大的，会不会导致后面更大的选不了了？
  - *硬币找零问题*：面值 [1, 3, 4]，要凑 6。
    - 贪心：先拿最大的 4，剩下 2，只能拿 1+1。总共 3 枚。
    - 实际最优：拿 3+3。总共 2 枚。
    - **结论**：贪心失效（因为拿了 4 导致没法拿两个 3），必须降级。

#### 第二步：能不能 DP/记忆化搜索？（中等，O(N^2)）

- 如果贪心失效（现在的选择会限制未来），但不需要知道具体的“路径”，只需要“最大值”。
- **特征**：存在“重叠子问题”。
- **比如**：0-1 背包问题。你选了这个物品，背包容量就少了，影响后面物品的选择，但你不需要排列组合所有顺序，只需要知道“剩多少容量”时的最大值。

#### 第三步：只能回溯？（最慢，O(2^N)）

- **特征**：数据量很小 ($N \le 20$)，或者题目要求输出**所有**可能的方案（不仅仅是最大值），或者约束条件极其复杂，没有任何规律可循。

# 

# DP—字符串/数组序列问题

### 1. 核心归纳：DP 数组含义如何确定？

在解决字符串或数组序列问题时，DP 状态定义通常逃不出以下 **三类套路**。判断属于哪一类的核心在于：**“当前元素是否必须与之前的元素发生直接关系（如大小比较、字符相等、连续性）？”**

#### 第一类：必须“以 i 结尾” (Ending at i)

**适用场景**：你需要依赖**末尾元素的值**或**连续性**来决定能否转移。

- **特征**：
  - 题目要求“连续子数组” (Subarray)。
  - 题目虽是“子序列” (Subsequence)，但有强烈的数值约束（如“递增”、“整除”）。
- **常用定义**：`dp[i]` 表示以 `nums[i]` (或 `s[i]`) **结尾** 的满足条件的解。
- **结果取值**：通常是 `max(dp[all])`，而不是 `dp[n-1]`。
- **典型题目**：
  - **300. 最长递增子序列 (LIS)**：需要比大小，必须知道末尾是谁。
  - **53. 最大子数组和**：要求连续，必须以 `i` 结尾才能决定能不能连上 `i+1`。
  - **674. 最长连续递增序列**：既要连续又要递增。

#### 第二类：代表“前 i 个元素” (First i elements)

**适用场景**：你只关心截至目前的**整体最优解**，不需要知道具体是谁结尾，或者可以通过简单的字符比较判断。

- **特征**：
  - 题目求“最长公共子序列”、“编辑距离”。
  - 当前元素 `i` 如果不匹配，可以直接继承 `i-1` 的结果。
- **常用定义**：`dp[i]` (或 `dp[i][j]`) 表示 `s[0...i]` (和 `t[0...j]`) 的最优解。
- **结果取值**：通常是 `dp[n]` (或 `dp[n][m]`)。
- **典型题目**：
  - **1143. 最长公共子序列 (LCS)** (你刚才做的那道)：如果 `s[i] != t[j]`，直接丢弃一个看之前的最大值，不需要知道末尾具体是啥。
  - **72. 编辑距离**。
  - **198. 打家劫舍**：选了 `i` 就不能选 `i-1`，但不强制以 `i` 结尾（虽然代码实现常类似于结尾思路，但本质是范围最优）。

#### 第三类：区间 DP `dp[i][j]` (Interval)

**适用场景**：问题是从两头向中间缩减，或者通过合并两个小区间得到大区间。

- **特征**：
  - 回文串问题（去头去尾还是回文）。
  - 矩阵连乘、石子合并。
- **常用定义**：`dp[i][j]` 表示从下标 `i` 到 `j` 的子串/子数组的最优解。
- **典型题目**：
  - **5. 最长回文子串**。
  - **516. 最长回文子序列**。

---

| **题目类型**             | **典型例题**    | **DP 状态定义** | **核心转移方程**                                            |
| ------------------------ | --------------- | --------------- | ----------------------------------------------------------- |
| **最长递增子序列 (LIS)** | LIS, 俄罗斯套娃 | **以 i 结尾**   | `if(nums[i]>nums[j]) dp[i] = max(dp[i], dp[j]+1)`           |
| **最长公共子序列 (LCS)** | LCS, 不相交的线 | **前 i, j 个**  | `s[i]==t[j] ? dp[i-1][j-1]+1 : max(dp[i-1][j], dp[i][j-1])` |
| **编辑距离**             | 编辑距离        | **前 i, j 个**  | `min(insert, delete, replace) + 1`                          |
| **最长回文子序列**       | 回文子序列      | **区间 [i, j]** | `s[i]==s[j] ? dp[i+1][j-1]+2 : max(dp[i+1][j], dp[i][j-1])` |

------

### 2. 你的“决策树” (Cheat Sheet)

下次看到序列 DP 题，按照这个流程思考：

1. **问自己：题目要求“连续”吗？**
   - 是 -> **必须定义为“以 i 结尾”** (如最大子数组和)。
   - 否 -> 进入下一步。
2. **问自己：如果不要求连续，那我需要知道“最后一个元素的值”来判断能否拼接吗？**
   - 是 (如 LIS, 需要比较 `nums[i] > nums[j]`) -> **定义为“以 i 结尾”**。
   - 否 (如 LCS, 只要 `text1[i] == text2[j]` 就行，不关心前面具体是谁) -> **定义为“前 i 个范围内的最优解”**。
3. 在图论和序列衔接问题中，只要涉及到“连接性”（Edge / Transition），状态定义必须包含“末端节点”的信息。
