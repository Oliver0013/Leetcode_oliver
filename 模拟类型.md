# 模拟类型

------

### 第一流派：时间轴与事件驱动 (Timeline & Event Driven)

核心特征：

题目涉及“时间”、“调度”、“资源分配”、“排队”。

痛点：时间是连续的，如果一秒一秒模拟会超时。

破局点：“离散化”时间。我们只关注“关键时间点”（开始时刻、结束时刻）。

**核心工具**：`Sort`（排序） + `PriorityQueue`（堆）。

**典型题目**：

- **LC 253. 会议室 II** (求最少需要几个会议室)
- **LC 1834. 单线程 CPU** (任务调度)
- **LC 218. 天际线问题** (扫描线算法，本质也是事件驱动)
- **LC 2402. 会议室 III** (你刚做过的)

**通用解法模板**：

C++

```
// 模板：时间轴/事件驱动模拟
void eventSimulation(vector<vector<int>>& events) {
    // 1. 排序：这是模拟时间流动的基石
    // 通常按【开始时间】排序
    sort(events.begin(), events.end());

    // 2. 定义容器：维护当前的状态
    // 如果是求最小值/最早结束 -> 小顶堆
    priority_queue<Type, vector<Type>, greater<Type>> pq;

    long long currentTime = 0;

    // 3. 遍历每一个事件（任务/会议）
    for (auto& event : events) {
        int startTime = event[0];
        
        // 【关键步骤 A】：推进时间
        // 在处理新事件前，必须把该时刻之前已经完成的旧事件清理掉
        while (!pq.empty() && pq.top().endTime <= startTime) {
            // 释放资源 / 记录结果
            pq.pop();
        }

        // 【关键步骤 B】：处理当前逻辑
        // 如果资源不够（比如CPU忙），可能需要等待 -> 更新 currentTime
        if (pq.empty()) {
             currentTime = max(currentTime, (long long)startTime);
        }
        
        // 【关键步骤 C】：将当前事件加入等待队列/处理队列
        pq.push({endTime, otherInfo});
    }
}
```

**实战案例 (LC 1834 单线程 CPU)：**

- **场景**：一堆任务 `[入队时间, 执行时间]`，CPU 一次只能做一个。
- **套模板**：
  1. 按入队时间排序。
  2. 用小顶堆存 `pair<执行时间, 索引>`（因为要选短任务）。
  3. `while` 循环：当 `currentTime >=` 任务入队时间，把任务全扔进堆里。
  4. CPU 从堆里拿一个做一个，`currentTime += 执行时间`。

------

### 第二流派：碰撞与消除 (Collision & Elimination)

核心特征：

物体在一个序列上移动，存在“相向而行”、“大吃小”、“左括号匹配右括号”的情况。

痛点：元素之间有连锁反应，撞完一个还能撞下一个。

破局点：栈 (Stack)。凡是涉及“消除”、“后来的先处理”，必用栈。

**典型题目**：

- **LC 735. 行星碰撞** (经典必做)
- **LC 2211. 统计道路上的碰撞** (你刚做过的)
- **LC 853. 车队** (追击问题)
- **LC 1047. 删除字符串中的所有相邻重复项**

**通用解法模板**：

C++

```
// 模板：栈式模拟 (碰撞/消除)
vector<int> stackSimulation(vector<int>& items) {
    vector<int> st; // 用 vector 模拟栈，方便最后取结果

    for (int item : items) {
        bool alive = true; // 标记当前元素是否“存活”

        // 【关键逻辑】：冲突检测循环
        // 当栈不为空，且 栈顶元素 与 当前元素 发生冲突 (如方向相反、大小满足消除条件)
        while (!st.empty() && isConflict(st.back(), item)) {
            if (st.back() < item) {
                // 栈顶比较弱，栈顶被消除
                st.pop_back();
                // 继续 while 循环，看看能不能消除下一个栈顶
            } else if (st.back() == item) {
                // 同归于尽
                st.pop_back();
                alive = false;
                break;
            } else {
                // 当前元素比较弱，自己挂了
                alive = false;
                break;
            }
        }

        // 如果经历了一轮厮杀还活着，就入栈
        if (alive) {
            st.push_back(item);
        }
    }
    return st;
}
```

**实战案例 (LC 735 行星碰撞)：**

- **场景**：正数向右，负数向左。
- **冲突条件**：`st.back() > 0` (栈顶向右) 且 `item < 0` (当前向左)。
- **套模板**：直接套用上面的 `while` 循环，谁绝对值小谁死。

------

### 第三流派：矩阵行军 (Matrix Walker)

核心特征：

在二维网格（Grid）里移动，如“螺旋矩阵”、“机器人扫地”、“生命游戏”。

痛点：if-else 写方向写得头晕，边界判断容易错。

破局点：方向数组 (Direction Array)。这是解决 2D 模拟题的唯一真神。

**典型题目**：

- **LC 54. 螺旋矩阵** (经典)
- **LC 59. 螺旋矩阵 II**
- **LC 48. 旋转图像** (原地修改)
- **LC 874. 模拟行走机器人**
- **LC 289. 生命游戏** (细胞状态机)

**通用解法模板**：

C++

```
// 模板：方向数组 (2D 模拟)
void matrixSimulation(int R, int C) {
    // 【神器】：方向数组 (右, 下, 左, 上)
    // 技巧：根据题目要求的旋转顺序定义
    int dirs[4][2] = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};
    int d = 0; // 当前方向索引 (0:右, 1:下...)

    int x = 0, y = 0;
    
    // 状态记录 (通常需要 visit 数组防止走回头路)
    vector<vector<bool>> visited(R, vector<bool>(C, false));

    for (int i = 0; i < totalSteps; i++) {
        // 记录当前位置
        visited[x][y] = true;
        // doSomething(x, y);

        // 【试探下一步】
        int nx = x + dirs[d][0];
        int ny = y + dirs[d][1];

        // 【撞墙检测】
        // 如果越界 或者 已经走过
        if (nx < 0 || nx >= R || ny < 0 || ny >= C || visited[nx][ny]) {
            // 换方向 (右转 90 度)
            d = (d + 1) % 4;
            // 重新计算下一步
            nx = x + dirs[d][0];
            ny = y + dirs[d][1];
        }

        // 真正的移动
        x = nx;
        y = ny;
    }
}
```

**实战案例 (LC 54 螺旋矩阵)：**

- **场景**：顺时针打印矩阵。
- **套模板**：定义 `dirs = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}}`。每次算出 `nx, ny`，如果撞墙或碰到 `visited`，就执行 `d = (d + 1) % 4` 换方向。

------

### 总结：拿到模拟题的“三板斧”

下次你看到模拟题（题目描述很长，让你复现过程），不要慌，按以下顺序排查：

1. **是不是涉及“时间”、“任务调度”？**
   - $\rightarrow$ **流派一：排序 + 优先队列**。
   - *心里默念：把事件排序，用堆维护当前可用的东西。*
2. **是不是涉及“一排东西”、“相撞”、“消除”？**
   - $\rightarrow$ **流派二：栈**。
   - *心里默念：后来的撞前面的，用栈顶代表前面的幸存者。*
3. **是不是在“棋盘”、“网格”里走来走去？**
   - $\rightarrow$ **流派三：方向数组**。
   - *心里默念：`dx`, `dy` 数组写好，撞墙就转弯。*

这三大类覆盖了 80% 的中等难度模拟题。剩下的 20% 通常是纯粹的逻辑翻译（比如“字符串排版 LC 68”），那种只能硬写，但没有数据结构难点。