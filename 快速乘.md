# 快速乘

将乘数 `z`二进制分解来实现乘法。要修正它，需要解决上述三个核心问题。正确的快速乘算法（`O(log n)`复杂度）逻辑如下，它通常用于在模运算中避免中间结果溢出：

1. **初始化**：结果 `res = 0`。
2. **循环**：当乘数 `z`大于 0 时继续循环。
3. **检查位**：如果 `z`的最低位是 1，则将当前的被乘数 `x`累加到结果 `res`中。
4. **加倍**：将被乘数 `x`加倍（左移一位等价于乘以2）。
5. **移位**：将乘数 `z`右移一位，准备检查下一位。

以下是修正后的代码示例，它正确地实现了快速乘，并用于判断 `z * y >= x`：

```cpp
bool quickAdd(int y, int z, int x){
    //x和y是负数，z是正数
    //需要判断z*y >= x是否成立
    int res = 0;
    int add = y;
    while (z != 0){
        //判断z的最低位是否为1
        if (z & 1){
            //加法下溢判断+提前判断剪枝
            if (res < x-add){
                //小到已经比x小了
                return false;
			}
			res += add;
        }
 		//如果z还不为1，说明需要为下一位准备翻倍的add
        if (z != 1){
            //提前剪枝+加法下溢判断
            //当前的y已经比x还要小了，加上原本的肯定更小
            if (add < x-add)){
                return false;            
            }
            //add放大两倍
            add += add;
        }
        //乘数右移一位（准备处理下一位）
        z = z>>1;		
    }
    return (res>=x)? true : false;
}
```