这是一套为你定制的**扫描线算法 (Sweep Line) 核心笔记**。这套笔记剥离了复杂的几何实现（线段树），专注于**面试高频考察的区间类问题**。

------

# 算法笔记：扫描线 (Sweep Line)

### 1. 核心定义

将**静态的区间重叠/覆盖问题**，转化为**动态的时间轴扫描问题**。

- **本质**：降维（2D $\rightarrow$ 1D）。
- **动作**：**排序 (Sorting)** 定序 $\rightarrow$ **遍历 (Scanning)** 维护状态。
- **核心比喻**：上帝视角看乱糟糟的线段 $\rightarrow$ 变成一根激光笔从左往右扫，只处理“碰到”的事件。

------

### 2. 两大流派 (面试必考)

根据关注点的不同，分为“计数派”和“贪心派”。

#### 流派一：事件点分解法 (Event Point / 差分思想)

- **适用场景**：求最大重叠层数、资源占用峰值、特定点被覆盖次数。
- **典型题目**：会议室 II (253)、拼车 (1094)。
- **核心逻辑**：
  1. **拆分**：将区间 `[start, end]` 拆为两个瞬时事件。
     - `start` $\rightarrow$ `+val` (上车/入场)
     - `end`   $\rightarrow$ `-val` (下车/离场)
  2. **排序**：按坐标从小到大。**若坐标相同，先下后上**（即 `-val` 排在 `+val` 前，视题目要求而定）。
  3. **累加**：遍历事件，`cur += change`，更新 `max`。

#### 流派二：区间贪心法 (Greedy Interval)

- **适用场景**：区间合并、计算总覆盖长度、判断是否无重叠。
- **典型题目**：合并区间 (56)、无重叠区间 (435)。
- **核心逻辑**：
  1. **不拆分**：保持区间完整性。
  2. **排序**：仅按 `start` 排序。
  3. **维护**：维护一个 `max_end`（当前延伸到的最远右边界）。
     - 若 `curr.start <= max_end`：相交，更新 `max_end = max(max_end, curr.end)`。
     - 若 `curr.start > max_end`：断开，归档旧区间，开启新区间。

------

### 3. 数据结构选择 (决策树)

做题时，根据**坐标范围**决定底层实现：

| **判断条件**                | **推荐算法**      | **数据结构**            | **复杂度**    | **备注**                              |
| --------------------------- | ----------------- | ----------------------- | ------------- | ------------------------------------- |
| **坐标范围小** ($\le 10^5$) | **差分数组 / 桶** | `vector<int> diff(MAX)` | $O(N + K)$    | **最快**。空间换时间 (如 1094 拼车)。 |
| **坐标范围大** ($10^9$)     | **标准扫描线**    | `vector<pair<int,int>>` | $O(N \log N)$ | **通用**。必须排序 (如 253 会议室)。  |
| **只求连通性** (不求层数)   | **区间贪心**      | `vector<vector<int>>`   | $O(N \log N)$ | 不需要拆点 (如 56 合并区间)。         |

------

### 4. 通用代码模板 (C++)

#### 模板 A：通用事件点 (适用于大范围坐标)

C++

```
// 适用于：会议室 II、天际线问题
int maxOverlap(vector<vector<int>>& intervals) {
    vector<pair<int, int>> events;
    for (auto& i : intervals) {
        events.push_back({i[0], 1});  // Start: +1
        events.push_back({i[1], -1}); // End:   -1
    }
    // 关键：Sort 默认先按坐标排，坐标相同按 value 排(-1 在前)
    sort(events.begin(), events.end());

    int cur = 0, ans = 0;
    for (auto& e : events) {
        cur += e.second;
        ans = max(ans, cur);
    }
    return ans;
}
```

#### 模板 B：差分数组 (适用于小范围坐标)

C++

```
// 适用于：拼车 (1094)
bool carPooling(vector<vector<int>>& trips, int capacity) {
    int diff[1001] = {0}; // 直接开桶
    for (auto& t : trips) {
        diff[t[1]] += t[0]; // Start
        diff[t[2]] -= t[0]; // End
    }
    int cur = 0;
    for (int i = 0; i <= 1000; i++) {
        cur += diff[i];
        if (cur > capacity) return false;
    }
    return true;
}
```

#### 模板 C：区间合并 (贪心)

C++

```
// 适用于：合并区间 (56)
vector<vector<int>> merge(vector<vector<int>>& intervals) {
    sort(intervals.begin(), intervals.end()); // 按 start 排序
    vector<vector<int>> res;
    res.push_back(intervals[0]); // 放入第一个作为基准

    for (int i = 1; i < intervals.size(); i++) {
        // 引用 res 的最后一个区间 (当前正在延伸的区间)
        if (intervals[i][0] <= res.back()[1]) {
            // 重叠 -> 融合 (更新右边界)
            res.back()[1] = max(res.back()[1], intervals[i][1]);
        } else {
            // 断开 -> 推入新区间
            res.push_back(intervals[i]);
        }
    }
    return res;
}
```

------

### 5. 易错点清单

1. **排序顺序**：
   - 如果题目要求“结束时刻立即释放资源”（如会议室），则 `end` 事件排在 `start` 之前（通过 `-1` 小于 `1` 自动实现）。
   - 如果题目要求“闭区间重叠”（如 `[1,2]` 和 `[2,3]` 算重叠），则 `start` 需排在 `end` 之前。
2. **上车下车逻辑**：
   - 注意题目给的是 `[start, end]` 还是 `[start, duration]`，一定要转化为绝对坐标。
   - 拼车问题中，到站下车意味着**先减后判**（如果同一站有人上下，先下人腾座，再上人）。

### 6. 刷题路径

1. **LeetCode 1094. 拼车** (差分数组，必做)
2. **LeetCode 56. 合并区间** (区间贪心，必做)
3. **LeetCode 253 / LintCode 919. 会议室 II** (标准扫描线，必做)
4. **LeetCode 732. 我的日程安排表 III** (进阶，动态扫描线)