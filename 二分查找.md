# 二分查找

| 题号 | 题名                   | 正确与否 | 作用                                        |
| ---- | ---------------------- | -------- | ------------------------------------------- |
| 2054 | 两个最好的不重叠的事件 | ×        | 找到后一个开始时间>前一个结束时间的最小元素 |
| 29   | 两数相除               | ×        | 找到满足情况的最大元素                      |



 本质是：**“减治思想”（Decrease and Conquer）—— 每次判断都能排除掉一半的错误答案。**

只要你的问题满足**“单调性”**（Monotonicity），或者能找到一种**“二段性”**（一边满足条件，另一边不满足），就可以用二分。

------

### 一、 二分查找适用于哪些问题？

主要可以分为三大类场景：

#### 1. 标准有序数组查找 

这是最基础的，也是你刚才遇到的场景。

- **特征**：数组是排好序的（或者可以通过 `sort` 变有序）。
- **目标**：找特定值，或者找特定值的边界（第一个大于/最后一个小于）。
- **例子**：
  - LeetCode 704. 二分查找（找 7 在哪里）
  - LeetCode 34. 排序数组中查找元素的第一个和最后一个位置（找 8 的左边界和右边界）
  - LeetCode 35. 搜索插入位置（也就是 `lower_bound`）

#### 2. 二分答案

这类题目数组**没有排序**，甚至**没有数组**。但是，**答案的范围是有序的**。

- **特征**：题目问“最小的 X 是多少”或“最大的 Y 是多少”。
- **逻辑**：如果你猜一个答案 $K$：
  - 如果 $K$ 能满足条件，那么 $K+1, K+2...$ 肯定也能满足（或者反过来）。
  - 这就构成了**单调性**。
- **例子**：
  - LeetCode 875. 爱吃香蕉的珂珂（如果你每小时吃 $K$ 根能吃完，那你每小时吃 $K+1$ 根肯定也能吃完。我们要找最小的 $K$）。
  - LeetCode 410. 分割数组的最大值。

#### 3. 寻找峰值/旋转数组

这类题目数组不是完全有序，但在**局部**是有序的，或者存在某种规律让我们能丢弃一半。

- **特征**：找峰值，或者在旋转排序数组中查找。
- **例子**：
  - LeetCode 33. 搜索旋转排序数组（我们之前讨论过的“断崖”）。
  - LeetCode 162. 寻找峰值（只要 `mid < mid+1`，说明峰值肯定在右边，左边可以丢弃）。

------

### 二、 二分查找通用解法

二分查找最容易写出死循环（Off-by-one error）。为了稳准狠，我建议你只记**两套模板**，分别对应两种情况。

#### 模板一：找准确值 (Exact Match)

适用场景：必须找到 nums[i] == target，找不到返回 -1。

特点：left <= right，mid + 1，mid - 1。

C++

```
int binarySearch(vector<int>& nums, int target) {
    int left = 0, right = nums.size() - 1; // 注意：right 是最后一个下标
    
    while (left <= right) { // 注意：这里有等号
        int mid = left + (right - left) / 2; // 防止溢出
        
        if (nums[mid] == target) {
            return mid; // 找到了，直接返回
        } 
        else if (nums[mid] < target) {
            left = mid + 1; // mid 太小，去右边 [mid+1, right]
        } 
        else {
            right = mid - 1; // mid 太大，去左边 [left, mid-1]
        }
    }
    return -1; // 没找到
}
```

#### 模板二：找边界 / 找第一个满足条件的数

- 找**左边界**（第一个 $\ge target$ 的数）。
- 找**右边界**（最后一个 $\le target$ 的数）。
- **二分答案**（找最小的可行解）。

这个模板的精髓在于：**不直接返回，而是用一个变量 `ans` 记录当前的“最佳嫌疑人”，然后继续尝试缩减范围。**

C++

```
// 例子：寻找第一个 >= target 的位置 (等同于 lower_bound)
int searchLeftBound(vector<int>& nums, int target) {
    int left = 0, right = nums.size() - 1;
    int ans = -1; // 用来记录目前找到的最优解
    
    while (left <= right) {
        int mid = left + (right - left) / 2;
        
        // 核心判断函数 check(mid)
        if (nums[mid] >= target) {
            // 1. 这是一个可能的答案，先记下来！
            ans = mid;
            // 2. 试探：左边还有没有更靠前的？
            // 既然 mid 满足条件，那比 mid 大的肯定也满足，我们要找最小的，所以往左缩
            right = mid - 1; 
        } 
        else {
            // 3. nums[mid] 小于 target，不够格，肯定在右边
            left = mid + 1;
        }
    }
    return ans;
}
```

left 指向第一个不满足条件的值，right 指向最后一个满足条件的值

#### 模板三：浮点数二分

**适用场景**：

- 几何切割、概率计算、求平方根。
- 题目要求的答案是小数，且误差允许范围在 $10^{-5}$ 左右。

**核心模板 (百次循环法)**：

```
// 模板三：浮点数二分
double binarySearchFloat(double left, double right) {
    // 技巧：直接循环 100 次
    // 原因：2^100 极其巨大，足以把区间切分到原子级精度，且绝对无死循环风险
    for (int i = 0; i < 100; i++) {
        double mid = left + (right - left) / 2.0;
        
        if (check(mid)) {
            right = mid; // 注意：浮点数是连续的，不能 -1，只能等于 mid
        } else {
            left = mid;  // 注意：不能 +1，只能等于 mid
        }
    }
    return left; // left, right, mid 此时几乎相等
}
```

**关键区别 (VS 整数二分)**：

1. **边界移动**：`L = mid` (不要 `+1`，否则会漏掉 `3.0` 到 `3.99` 这一大片)。
2. **终止条件**：`for(100)` (比 `while(R-L > 1e-5)` 更稳，防死循环)。
3. **变量类型**：全员 `double`，防止整数截断和溢出。



### 三、 总结：如何套用？

当你拿到一道题，想要用二分时，按这个步骤思考：

1. **构造单调性**：
   - 如果是找数：先排序。
   - 如果是二分答案：确定“答案越大越可能满足”还是“答案越小越可能满足”。
2. **定义 `check(mid)` 函数**：
   - `if (nums[mid] >= target)`：这是最简单的 check。
   - 在复杂题（如“爱吃香蕉的珂珂”）中，`check(k)` 就是写一个函数判断“速度 k 能不能吃完”。
3. **套用模板二**：
   - 如果 `check(mid)` 成功：记录 `ans = mid`，然后**贪心**地去尝试更好的那边（比如找更小的解，就 `right = mid - 1`）。
   - 如果 `check(mid)` 失败：老老实实去另一边（`left = mid + 1`）。

只要掌握了**模板二**（记录 `ans` + 移动边界），你就掌握了二分查找的 90%，包括你之前困惑的 `upper_bound` 其实就是在这个逻辑上运行的。